# Background Remover API - Production Configuration Template
# SECURITY WARNING: Copy to .env and customize sensitive values!
# DO NOT commit actual .env file to version control!

# =============================================================================
# FLASK CONFIGURATION (PRODUCTION)
# =============================================================================
FLASK_APP=app.py
FLASK_ENV=production
DEBUG=False
HOST=0.0.0.0
PORT=5001

# =============================================================================
# APPLICATION INFO (PRODUCTION)
# =============================================================================
APP_NAME=Background Remover API
APP_VERSION=1.0.0

# =============================================================================
# RATE LIMITING CONFIGURATION (PRODUCTION - STRICT)
# =============================================================================
RATE_LIMIT_DEFAULT_PER_HOUR=500
RATE_LIMIT_DEFAULT_PER_MINUTE=50

# Stricter limits for production (per minute)
RATE_LIMIT_REMOVE_BG_PER_MINUTE=5    # Heavy processing - very strict
RATE_LIMIT_PREVIEW_PER_MINUTE=8      # Preview - moderate
RATE_LIMIT_BASE64_PER_MINUTE=6       # Base64 - moderate
RATE_LIMIT_HEALTH_PER_MINUTE=100     # Health checks - lenient
RATE_LIMIT_INFO_PER_MINUTE=30        # API info - lenient

# Rate limiting storage
RATE_LIMIT_STORAGE=redis

# Redis Configuration (SECURE YOUR REDIS INSTANCE!)
REDIS_URL=redis://localhost:6379/0
REDIS_DB=0

# =============================================================================
# FILE UPLOAD CONFIGURATION
# =============================================================================
MAX_CONTENT_LENGTH=16777216  # 16MB in bytes
UPLOAD_FOLDER=uploads
OUTPUT_FOLDER=outputs

# =============================================================================
# SECURITY CONFIGURATION (CRITICAL - CUSTOMIZE!)
# =============================================================================
# IMPORTANT: Replace with your actual domains in production!
CORS_ORIGINS=https://yourdomain.com,https://app.yourdomain.com

# =============================================================================
# LOGGING CONFIGURATION
# =============================================================================
LOG_LEVEL=WARNING

# =============================================================================
# PERFORMANCE CONFIGURATION
# =============================================================================
CLEANUP_INTERVAL_HOURS=1

# =============================================================================
# ADDITIONAL PRODUCTION SECURITY (UNCOMMENT AND CONFIGURE)
# =============================================================================
# Add these as needed for your production setup:

# API Keys (if using external services)
# API_KEY_SECRET=your_secret_api_key_here
# API_KEY_ENCRYPTION_KEY=your_32_char_encryption_key

# Database (if using database storage)
# DATABASE_URL=postgresql://user:password@localhost:5432/bgremover
# DATABASE_PASSWORD=secure_database_password

# SSL/TLS (if using HTTPS with self-signed certs)
# SSL_CERT_PATH=/path/to/cert.pem
# SSL_KEY_PATH=/path/to/key.pem

# Monitoring (if using monitoring services)
# SENTRY_DSN=https://your-sentry-dsn-here
# MONITORING_API_KEY=your_monitoring_api_key

# =============================================================================
# PRODUCTION DEPLOYMENT CHECKLIST
# =============================================================================
# Before deploying to production:
#
# [ ] Replace CORS_ORIGINS with actual domains
# [ ] Secure Redis instance with password
# [ ] Set up SSL/TLS certificates
# [ ] Configure firewall rules
# [ ] Set up monitoring and logging
# [ ] Test rate limiting under load
# [ ] Verify file upload security
# [ ] Set up backup strategy
# [ ] Configure reverse proxy (nginx/apache)
# [ ] Test failover procedures
# [ ] Review error handling
# [ ] Set up log rotation
# [ ] Test with actual production data